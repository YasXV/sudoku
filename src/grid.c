#include <stdlib.h>
#include <stdio.h>
#include "grid.h"



//a_grid init_grid(){}
//creation of the sudoku grid and memory allocation
/*	a_grid my_grid = (a_grid)malloc(sizeof(*my_grid));
	
	//checking
	if (my_grid == NULL) {
        fprintf(stderr,"fail to allocate memory to a sudoku grid");
        return NULL;
    	}

    //init of dimension
    my_grid->dimension=3;    
    int n = my_grid->dimension;
    
    //init of matrix
    my_grid->matrix = (a_grid)malloc(n*sizeof(a_table));
    for(int i=0; i<n; i++){
    	(my_table->matrix)[i] = (table*)malloc(n*sizeof(table));
    }
    
    //checking if the matrix
	if (my_table->matrix == NULL) {
        fprintf(stderr,"fail to allocate memory to the matrix of a table");
        return NULL;
    	}
    	
    }*/
